[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto

[tool:pytest]
addopts = -v --tb=short

[coverage:run]
source = agents,tools,core,services,security,utils,api,pipeline
omit = tests/*,.venv/*

[coverage:report]
fail_under = 70
show_missing = true
skip_covered = false
