The HITL workflow primitives in this package are available for future human-review integration, but they are not wired into the default production workflow path.

The core production graph currently uses the automated review step and audit trail in schemas/state.py. The approval manager remains available for manual or future integrations.
