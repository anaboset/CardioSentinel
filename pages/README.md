This directory contains the legacy Streamlit UI implementation used for local demos and manual review workflows.

The production deployment now serves the FastAPI-based experience from api/main.py. The pages here are intentionally kept for compatibility and local development, but they are not part of the primary production runtime.
